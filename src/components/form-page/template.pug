main.form-container
    div.form-inner
        h1.form-title= title
        template(data-component="form")
        footer.form-footer
            p= footerText
            a.link(href=link)= linkText
