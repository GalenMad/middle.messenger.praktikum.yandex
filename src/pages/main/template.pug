main.content
    div.content-inner
        h1=name
        p Страницы
        ul 
            each item in list
                li
                    a.link(href="../" + item + "/index.html") #{item}

